# Revision Log

| Date | Change | Author |
|---|---|---|
| 2026-07-04 | Created demo-video workspace + 11 pre-approval planning deliverables. Reviewed app (routes, TADIS, defects, NSC inspection), existing screenshots, Remotion `video-generator`, demo seed. No app/DB/website changes. Awaiting approval + answers to Q1–Q7. | Claude Code |
| 2026-07-04 | **Approvals received:** Q1 = OpenMontage (but not present in env — access pending). Q2 = seed ABC Logistics on local/staging (approved). Q3 = show real Scene 7 actions, rest as notes (approved). Verified OpenMontage absent (no global pkg / CLI / node_modules / repo ref). **HOLDING** execution: (a) render tool pending OpenMontage source or Remotion fallback; (b) will not run seed until DATABASE_URL target is confirmed local/staging (safety: no production DB). | Claude Code |
| 2026-07-04 | OpenMontage sourced (github.com/calesthio/OpenMontage), installed at `C:/Users/dkwes/OpenMontage` (venv + piper-tts; node deps + .env pre-existing). Preflight PASSED: Remotion ✓, FFmpeg ✓, HyperFrames ✗ (npm unresolvable → Remotion locked, HF logged rejected). ElevenLabs TTS + image/stock-video gen available. **Decisions:** capture source = Option C (no DB writes; screenshots + curated coolant TADIS case + Remotion animation + stock b-roll). Narration = ElevenLabs. Pipeline = screen-demo. Starting `idea` stage. | Claude Code |
| 2026-07-04 | Stages idea→script→scene_plan authored, schema-validated, and checkpointed (idea/script/scene_plan approved; 9 scenes, 185s). Fixed an SSL blocker: a TLS-inspection proxy re-signs API hosts with a Windows-store root not in certifi → installed `truststore` + venv `sitecustomize.py` so provider APIs verify. Generated first ElevenLabs voice sample (George, eleven_multilingual_v2) → delivered to user for approval before full narration. Assets stage in progress. | Claude Code |
| 2026-07-04 | Audio approved. Assets built: 9 ElevenLabs narration clips → stitched to timeline-aligned narration.mp3 (182.8s); Pexels HD truck b-roll; Pixabay music bed; 17-cue SRT/VTT (ASCII-clean). AI image gen unavailable (FAL/Imagen 403, OpenAI SDK missing) → Scene 1/4/8 graphics handled natively in Remotion. Compose: authored 16:9 props (brand navy/red, 12 cuts/9 scenes, section titles, caption callouts, narration+ducked music). Smoke render (frames 0-90) PASSED — clean 1080p, verified frame. Full 16:9 master render running. | Claude Code |
| 2026-07-05 | **PACKAGE COMPLETE.** Master re-rendered clean (187s, 1080p). Post-processed to: narrated+captioned 16:9 (17.4MB), silent/captioned 16:9 (15.4MB), web mp4 (10.2MB), web webm (8.4MB), poster. Burned captions verified. OpenMontage stages idea→publish all checkpointed. Deliverables in demo-video/09_exports, /10_thumbnails, /08_captions, /11_distribution. Known cosmetic nits logged (CTA grey bg, cyan hero highlight); optional 9:16/1:1 not yet rendered. | Claude Code |
| 2026-07-05 | (v1 detail) First full master rendered. Frame audit found problems: several source screenshots were captured empty/logged-out — `12-defect-flagged` and `13-ai-diagnostic-intake` are ERROR pages (Access Denied), and all "AI result" shots are actually the empty intake form. No truthful screenshot exists for the AI-triage RESULT or the manager DECISION. **User decisions:** (1) portray hero scenes 5 & 7 as clearly-labeled Remotion summary cards (no fabricated UI); (2) minimize zero-state dashboards, lean on strong real frames (Truck #42 maintenance record, mobile inspection). Switched captions to burned-in SRT (callout_balloons weren't rendering). Reworked props; verified new hero card frame. Distribution copy + thumbnail + live demo script written. Re-render all-frames OK but Remotion's bundled ffmpeg stitch crashed (0xC0000142, transient DLL-init) — retrying. | Claude Code |
